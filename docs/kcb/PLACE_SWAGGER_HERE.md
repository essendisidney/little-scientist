# Drop official portal export here

Official **MpesaExpressAPIService** OpenAPI is already in this folder:

- [`mpesa-express.swagger.json`](./mpesa-express.swagger.json)

Contract mapping: [CONTRACT.md](./CONTRACT.md).

If KCB publishes a newer export, replace that file and re-check headers (`routeCode`, `operation`, `messageId`) and `STKPushRequest` field lengths.
